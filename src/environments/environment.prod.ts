export const environment = {
  production: true,
  firebase: {
    // production firebase config if needed
  },
  // Docker-Compose: gleiche Domain/Port, Nginx proxyt /api auf n8n
  apiUrl: '/api/'
};
